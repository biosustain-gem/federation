name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install dep
      run: npm install --save-dev --ignore-scripts install-peers && npm install
    - name: Build
      run: npm run build
